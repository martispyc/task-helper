# Projects

One folder per code project — created by the Context Getter on first use:
`projects/<project>/tasks/<TICKET-KEY>/context.md` plus an optional `projects/<project>/project.md`.

This folder belongs to YOUR TEAM. Tool updates (git pull upstream main) never touch it.
